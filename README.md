# juggling_rewards

An example of a stable two-ball-one-arm toss juggling pattern to demonstrate some candidate reward functions for robot juggling.

- survival bonus
- control cost
- penalty on small ball distances, to discourage holding multiple balls at once

Pip install `mujoco`, then run main.py.
